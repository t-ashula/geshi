{
  "name": "@geshi/logger",
  "version": "0.1.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "lint": "eslint ./src/**/*.ts ./test/**/*.ts",
    "format": "prettier --write .",
    "test": "vitest run"
  },
  "dependencies": {
    "pino": "^9.6.0",
    "pino-pretty": "^13.0.0"
  }
}
