{
  "name": "@geshi/logger",
  "version": "0.1.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "lint": "ESLINT_USE_FLAT_CONFIG=false eslint ./{src,test}/**/*.ts",
    "format": "prettier --write .",
    "test": "vitest run"
  },
  "dependencies": {
    "pino": "^9.7.0",
    "pino-pretty": "^13.0.0"
  }
}
