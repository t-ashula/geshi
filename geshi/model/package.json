{
  "name": "@geshi/model",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint ./{src,test}/**/*.ts",
    "format": "prettier --write .",
    "prisma:generate": "prisma generate"
  },
  "dependencies": {
    "@prisma/client": "^7.3.0"
  },
  "devDependencies": {
    "prisma": "^7.2.0"
  }
}
