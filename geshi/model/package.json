{
  "name": "@geshi/model",
  "version": "1.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "lint": "eslint ./src/**/*.ts ./test/**/*.ts",
    "format": "prettier --write .",
    "test": "vitest run"
  },
  "dependencies": {
    "@prisma/client": "^6.6.0",
    "pg": "^8.14.1"
  },
  "devDependencies": {
    "prisma": "^6.6.0"
  }
}
