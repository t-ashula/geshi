{
  "name": "@geshi/model",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint ./src/**/*.ts ./test/**/*.ts",
    "format": "prettier --write ."
  },
  "dependencies": {
    "@prisma/client": "^6.6.0",
    "typescript": "^5.3.3"
  },
  "devDependencies": {
    "prisma": "^5.10.0",
    "vitest": "^3.1.1"
  }
}
